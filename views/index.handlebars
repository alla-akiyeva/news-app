
{{!-- <table>
<thead>
    <th>Headline</th>
    <th></th>
</thead>
<tbody>
    <tr>
        <td><a class="waves-effect waves-light btn-large">Scrape News</a></td>
        <td><a class="waves-effect waves-light btn-large">Clear</a></td>
    </tr>
    {{#each article}}
    <tr>
        <td><a href="{{this.link}}">{{this.title}}</a></td>
        <td><a class="btn-small">Save</a></td>
    </tr>
    {{/each}}

</tbody>
</table> --}}

<div class="container">
{{!--Image Card--}}
<div class="row">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title">Mongo Scraper</span>
          <p>WSJ | Tech</p>
        </div>
        <div class="card-action">
          <a class="waves-effect waves-light btn">Scrape</a>
          <a class="waves-effect waves-light btn">Saved Articles</a>
          <a class="waves-effect waves-light btn">Clear</a>
        </div>
      </div>
    </div>
  </div>


    {{!-- <ul class="collection">
        {{#each article}}
        <li class="collection-item"><div>{{this.title}}<a href="{{this.link}}" class="secondary-content"><a class="btn-small">Save</a></div></li>
        {{/each}}
    </ul> --}}

    <table>
        <tbody>
            {{#each article}}
            <tr>
                <td><a href="{{this.link}}">{{this.title}}</a></td>
                <td><a class="btn-small">Save</a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    {{!-- {{#each article}}
    <div class="row">

        <div class="column">
            <td><a href="{{this.link}}">{{this.title}}</a></td>
        </div>

        <div class="column">
            <td><a class="btn-small">Save</a></td>
        </div>

    </div>
    {{/each}} --}}

    </div>
      

